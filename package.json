{
  "name": "socialmedia_dashboard",
  "version": "1.0.0",
  "engines": {
    "node": "7.4.0",
    "npm": "4.1.1"
  },
  "scripts": {
    "postinstall": "cd client && npm install",
    "rails-server": "echo 'visit http://localhost:3000/' && foreman start -f Procfile.dev",
    "test": "rspec && cd client && npm run test",
    "linters": "rubocop -a && cd client && npm run eslint"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dsznajder/socialmedia_dashboard.git"
  },
  "license": "MIT"
}
